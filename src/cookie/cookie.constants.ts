// Name of the cookie used to store the JWT access token for authenticated sessions
export const ACCESS_TOKEN_COOKIE = 'access_token';

// Name of the CSRF token cookie set by the server and readable by client-side JavaScript
export const XSRF_COOKIE = 'XSRF-TOKEN';

// Name of the request header where the client echoes back the XSRF token for CSRF validation
export const XSRF_HEADER = 'x-xsrf-token';
